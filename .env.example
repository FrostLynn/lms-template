# Environment template untuk Docker Compose.
# Salin ke file `.env` lalu sesuaikan: cp .env.example .env

# Image Moodle dari GHCR (ganti owner dengan username GitHub kamu).
# Atau: MOODLE_IMAGE=moodle-dev:local untuk pakai image build lokal.
MOODLE_IMAGE=ghcr.io/frostlynn/lms-template:latest
